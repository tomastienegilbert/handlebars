{{!-- punto 3 --}}
<div class="col-4 mb-3">
    <div class="card">
        <img src="/assets/imgs/{{ this.nombre}}.png" class="card-img-top" alt="...">
        <div class="card-body">
            <h5 class="card-title">{{ this.nombre }}</h5>
            <a href="#" class="btn btn-primary">Agregar a carrito</a>
        </div>
    </div>
</div>